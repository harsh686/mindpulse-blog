# Core AI orchestration
marvin>=2.0

# LLM providers — NVIDIA is used by default (free)
# openai SDK is used for NVIDIA's OpenAI-compatible endpoint
openai>=1.30.0

# Optional alternative providers
google-generativeai>=0.8.0
groq>=0.9.0

# Web server
flask>=3.0.0
markdown>=3.6

# Utilities
python-dotenv>=1.0.0
schedule>=1.2.0
